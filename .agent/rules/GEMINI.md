# ULTRA TOKEN-EFFICIENT OPERATING RULES

1. NO REPO DUMPS: Never run recursive directory listings or repo-wide grep.
2. SURGICAL TARGETING: Identify target files by exact symbol name. Open ONLY the file to edit.
3. BARREL IMPORTS: Always import UI components from `@/components/ui`.
4. NO UNNECESSARY REREADS: Never reread files whose exports and structure are already known.
5. FAST VALIDATION: Validate TypeScript using `npx tsc --noEmit` before running slower full builds.
6. CONCISE LOGS: Never write multi-paragraph summaries in logs or progress files. 1 bullet is sufficient.
